import PageTitle from '../../components/PageTitle';
import './styles.scss';

const LoginTitle = () => {
    return <PageTitle title='Login' customClass='login-title' />;
};

export default LoginTitle;
